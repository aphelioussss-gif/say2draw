# OpenAI API Configuration
# Required for LLM fallback feature
OPENAI_API_KEY=sk-your-api-key-here

# Optional: OpenAI model to use (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: Server port (default: 3001)
PORT=3001
